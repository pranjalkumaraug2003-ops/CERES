# CERES backend — core runtime. Python 3.12.
#
#   python -m venv server/venv
#   server/venv/Scripts/pip install -r requirements.txt
#   server/venv/Scripts/playwright install chromium
#
# On-device speech/vision models are optional — see requirements-local.txt.

# ── Web framework / server ────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.47.0
python-multipart==0.0.29          # UploadFile on /api/stt
websockets==16.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1

# ── Datastores ────────────────────────────────────────────────────────────────
qdrant-client==1.18.0             # vector memory
redis==7.4.0                      # session history + intent cache
SQLAlchemy==2.0.49
asyncpg==0.31.0                   # Postgres driver

# ── Gemini / Google APIs ──────────────────────────────────────────────────────
google-genai==1.75.0              # TTS in voice_service
langchain-google-genai==4.2.3
langchain-core==1.4.0
google-api-python-client==2.196.0 # Gmail + Calendar
google-auth==2.53.0
google-auth-oauthlib==1.4.0

# ── Embeddings: vector memory + intent routing ────────────────────────────────
# NOTE: pulls in torch (~2 GB), the bulk of the install. Required today because
# qdrant_service.py builds a SentenceTransformer at module import time, so it
# loads even when nothing needs it. Once that is made lazy, this moves to
# requirements-local.txt and a cloud-only install gets much smaller.
sentence-transformers==5.5.1
torch==2.12.0
numpy==1.26.4

# ── Tools ─────────────────────────────────────────────────────────────────────
playwright==1.60.0                # browser automation (needs `playwright install`)
beautifulsoup4>=4.12,<5           # web-search parsing; regex fallback if absent
psutil==7.2.2                     # CPU/RAM/battery stats
pillow==12.2.0                    # screenshots via PIL.ImageGrab
num2words==0.5.14                 # digits → words before TTS
